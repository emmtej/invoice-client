# Local Development Port
CLIENT_PORT=3000

# Required for login/register in local dev (matches CI)
# If true, authentication logic will use mock/local bypass
VITE_DEMO_MODE=true

# Optional: Set to true to open bundle visualizer after build
# ANALYZE=true
